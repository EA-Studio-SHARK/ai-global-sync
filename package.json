{
  "name": "ai-global-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first AI tool rules and Skills synchronizer.",
  "type": "module",
  "bin": {
    "ai-global-sync": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/test/*.test.js",
    "test:src": "node --test --loader ts-node/esm test/*.test.ts",
    "start": "node dist/index.js",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
